| 요소 | 출력 필드 | 산출 위치 | 툴팁 문구 |
|---|---|---|---|
| **블록 A — 나에게 관련** | `blocks.A` | `score.block_a()` | 최근 30일 동안 내가 나눈 대화를 의미 단위로 바꿔 평균을 낸 뒤, 각 주제와 얼마나 가까운지 비교합니다. 여기에 우리 부서가 그 주제를 얼마나 물었는지와 최근에 생긴 주제인지를 더해 순위를 냅니다. 최근 대화가 3건보다 적으면 판단할 근거가 부족해 표시하지 않습니다. |
| **블록 B — 급상승 주제** | `blocks.B` | `score.gate_b()` + `block_b()` | 지난주보다 이번 주에 질문이 크게 늘어난 주제입니다. 이번 주 10회 이상, 지난주 대비 2배 이상, 서로 다른 5명 이상이 물어본 주제만 올라옵니다. 세 조건을 모두 넘어야 하므로 한 사람이 여러 번 물어서 늘어난 주제는 걸러집니다. |
| **블록 C — 많이 본 주제** | `blocks.C` | `score.gate_c()` + `block_c()` | 이번 주에 여러 사람이 반복해서 찾은 주제입니다. 이번 주 5회 이상, 서로 다른 3명 이상이 물어본 주제 중에서 얼마나 많은 사람에게 퍼졌는지를 가장 크게 보고 고릅니다. 우리 부서가 많이 물어본 주제일수록 위로 올라옵니다. |
| **이번 주 질의** | `query_count` | `aggregate.Cluster.cnt_this` | 최근 7일 동안 이 주제로 들어온 질문 수입니다. 회사 전체 기준이며, 대화를 새로 시작하는 첫 질문만 셉니다. '더 자세히' 같은 이어지는 질문과 잡담은 빼고 셉니다. |
| **질의한 사람** | `user_count` | `aggregate.Cluster.usr_this()` | 최근 7일 동안 이 주제를 물어본 사람 수입니다. 같은 사람이 여러 번 물어도 1명으로 셉니다. 질문 수보다 이 숫자를 더 중요하게 봅니다 — 한 사람이 반복해서 만든 숫자는 브리핑에 올리지 않기 때문입니다. |
| **지난주 대비** | `ratio` | `aggregate.Cluster.ratio()` | 이번 주 질문 수를 지난주 질문 수로 나눈 값입니다. 지난주에 한 건도 없었다면 나눌 수 없으므로 배수 대신 '신규'로 표시합니다. |
| **점수** | `score` | `score.block_b/​c()` 의 `score_fn` | 오늘 브리핑에서 어떤 주제를 먼저 보여줄지 정하는 순위 값입니다. 늘어난 정도, 질문 수, 퍼진 정도를 정해진 비율로 합쳐 계산합니다. 오늘 목록 안에서만 비교되는 값이라 어제 점수와 비교하는 의미는 없습니다. |
| **주간 비교** | `prev_count` / `query_count` | `aggregate.build()` | 지난 7일과 그 앞 7일의 질문 수를 나란히 놓은 것입니다. 막대 길이는 두 값 중 큰 쪽을 가득 찬 기준으로 맞췄습니다. |
| **급상승 / 신규 뱃지** | `badge` | `score.block_b()` 의 `badge_fn` | 지난주에도 있었는데 이번 주에 늘었으면 '급상승', 지난주에 한 건도 없다가 이번 주에 처음 나타났으면 '신규'입니다. |
| **근거 범위** | `evidence_scope` / `fallback` | `score._select_with_fallback()` | 이 목록을 어느 범위에서 골랐는지 알려줍니다. 우리 부서 질문만으로 개수가 차면 '부서', 모자라면 본부, 그래도 모자라면 전사까지 넓혀 채웁니다. 표시된 숫자는 범위와 상관없이 언제나 회사 전체 기준입니다. |
| **질의 이력** | `queries[]` / `query_total` | `score.build_queries()` | 실제로 들어온 질문 문장을 최근 순으로 보여줍니다. 같은 문장은 하나로 합쳐 최대 5건까지 전달합니다. 누가 물었는지는 담지 않으며, 사번이나 문서번호처럼 보이는 숫자는 가려서 표시합니다. |

동작: 클릭하면 열리고 다시 누르면 닫힌다. 한 번에 하나만 열리며 바깥을 클릭하거나
`Esc` 를 누르면 닫힌다. 버튼은 `aria-expanded`, 설명은 `role="tooltip"` 과
`aria-describedby` 로 연결해 스크린리더에서도 읽힌다. 화면 오른쪽에 있는 버튼은
설명이 밖으로 넘치지 않도록 오른쪽 정렬로 열리고, 지표 설명은 설명 대상 숫자를
가리지 않도록 값 아래에 열린다.